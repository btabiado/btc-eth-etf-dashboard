# Minimum supported Python: 3.10
# (Bumped from 3.9 so we can take Dependabot-patched requests/pytest.)
anthropic==0.101.0
numpy==2.0.2
pandas==2.3.3
pytest>=8,<9
python-dateutil==2.9.0.post0
requests==2.32.5
Flask==3.1.3

# LTHCS Phase 1 (Week 1: schemas + validate)
pydantic==2.13.4
python-dotenv==1.2.2
# LTHCS Phase 1 (Week 2: data sources)
yfinance==1.2.0
# LTHCS Phase 1 (Week 3: Adoption pillar — Google Trends)
pytrends==4.9.2
# LTHCS Phase 5 (Tier 5 #26: MCP server)
# mcp[cli]>=1.0 requires Python >=3.10 — gate it so the production cron
# workflows on Python 3.9 don't hard-fail at install time. The MCP server
# itself only runs locally on demand (not in any cron path); install via
# `pip install 'mcp[cli]>=1.0'` in a Python-3.10+ venv when needed.
mcp[cli]>=1.0; python_version >= "3.10"
