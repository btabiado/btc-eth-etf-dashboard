# Minimum supported Python: 3.10
anthropic==0.105.2
numpy==2.4.6
pandas==2.3.3
# pytest 9.0.3 patches GHSA-6w46-j5rx-g56g (local-privesc via predictable
# /tmp/pytest-of-{user} tmpdir on UNIX). All earlier 8.x/9.x affected.
pytest==9.0.3
python-dateutil==2.9.0.post0
# requests 2.33.0 preserves the GHSA-gc5v-m9x4-r6x2 security bump (commit 3e8d026).
requests==2.34.2
Flask==3.1.3

# LTHCS Phase 1 (Week 1: schemas + validate)
pydantic==2.13.4
# python-dotenv 1.2.2: the set_key symlink code path (GHSA-mf9w-mj56-hr94) is
# UNUSED in this repo (read-only).
python-dotenv==1.2.2
# LTHCS Phase 1 (Week 2: data sources)
yfinance==1.4.1
# LTHCS Phase 1 (Week 3: Adoption pillar — Google Trends)
pytrends==4.9.2
# LTHCS Phase 5 (Tier 5 #26: MCP server)
mcp[cli]==1.27.1

# V2 dashboard CI guard — used by tools/validate_v2_dashboard.py to actually
# parse the inline app JS via embedded V8 instead of regex-only heuristics.
# Catches the class of bug that bricked V2 in prod on 2026-05-25 (unary-plus
# syntax error) which pattern-only checks couldn't see. ~30MB wheel,
# parse-only (no script execution) — adds ~1-2s to the validate step.
mini-racer==0.14.1
